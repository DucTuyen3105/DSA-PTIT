#include<bits/stdc++.h>
using namespace std;
int n,m,s;
vector<int>ke[1000];
void nhap()
{
	cin>>n
int check(int u)
{
	for(auto x : ke[u])
	{
		if(color[x]==c)
		{
			return false;
		}
	}
	return true;
}
bool Try(int u)
{
	if(u>v)
	{
		return true;
	}
	for(int i = 1 ; i <= s ; i++)
	{
		color[u] = i
		
